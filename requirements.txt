# Runtime dependencies.
# torch is intentionally unpinned to a CUDA build: install the wheel that matches
# your machine first (see README "GPU"), then install the rest of this file.
numpy>=1.26
pydantic>=2.6
pyyaml>=6.0
sentence-transformers>=3.0
transformers>=4.40
torch>=2.2
httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.29
tqdm>=4.66

# Dev / eval
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4

# Docs: scripts/make_overview_pdf.py renders the project overview, reading its
# figures live from runs/ so the PDF cannot drift from what was measured.
reportlab>=4.0
